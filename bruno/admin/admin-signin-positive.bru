meta {
  name: Admin Signin - Positive
  type: http
  seq: 39
  res.status: 200
}

post {
  url: {{baseUrl}}/auth/signin
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "password": "admin123"
  }
}

script:post-response {
  if (res.status === 200) {
    const body = res.getBody();
    bru.setVar("adminToken", body.token);
    bru.setVar("adminId", body.user.id.toString());
    console.log("Admin signin successful.");
  }
}

assert {
  res.status: 200
  body.token: "exists"
  body.user.role: "Admin"
}