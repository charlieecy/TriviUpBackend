meta {
  name: Signup - Negative - Invalid Email
  type: http
  seq: 6
  res.status: 400
}

post {
  url: {{baseUrl}}/auth/signup
  body: json
  auth: none
}

body:json {
  {
    "username": "validuser",
    "email": "notanemail",
    "password": "{{testPassword}}"
  }
}

assert {
  res.status: 400
}